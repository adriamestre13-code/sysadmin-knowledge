#  Configuración y gestión de copias de seguridad en Windows Server 2022

En un entorno corporativo, la continuidad del negocio y la integridad de la información dependen directamente de la capacidad de responder ante incidentes de pérdida de datos. Fallos de hardware en los servidores, corrupción de sistemas de archivos, infecciones por malware o errores humanos son situaciones comunes que exigen disponer de mecanismos de protección eficaces.

Esta guía documenta la puesta en marcha de un laboratorio práctico basado en Windows Server 2022, enfocado en el despliegue, configuración y automatización de sistemas de respaldo mediante la herramienta nativa Windows Server Backup.

---

## ¿Qué es una copia de seguridad y para qué sirve?

Una copia de seguridad (o *backup*) es un duplicado de la información crítica (ficheros, volúmenes o el estado completo del sistema) que se almacena en una unidad o soporte independiente de la fuente original.

Su función principal es garantizar la disponibilidad de los datos, permitiendo restaurar la información a un estado operativo previo en caso de una contingencia o desastre. De este modo, se asegura la continuidad de los servicios informáticos y se minimizan las interrupciones en la actividad empresarial.

---

## Objetivos del Laboratorio

En esta sección del laboratorio se trabajarán los siguientes objetivos prácticos:

- Preparación del almacenamiento dedicado: Incorporación e inicialización de un disco de 50 GB con esquema de particiones MBR y formato NTFS destinado a albergar los respaldos.
- Instalación de componentes: Habilitación de la característica Windows Server Backup a través de comandos de PowerShell (`Install-WindowsFeature`).
- Creación de datos de prueba: Estructuración de directorios y archivos de prueba en el volumen del sistema para validar el proceso de respaldo y recuperación.
- Copia de seguridad puntual (Backup Once): Configuración y ejecución de un respaldo ad-hoc de elementos específicos del disco.
- Restauración de datos (Recovery): Simulación de pérdida de datos mediante borrado y posterior recuperación puntual de archivos y carpetas desde el archivo de backup.
- Planificación automatizada (Backup Schedule): Configuración de directivas de backup del sistema completo (*Full server*) de forma programada.
- Ajuste de periodicidad con el Programador de tareas: Modificación de los desencadenadores (*Triggers*) en la biblioteca de tareas de Windows para establecer ejecuciones periódicas personalizadas.

---

## Paso 1. Preparación del almacenamiento dedicado

Para poder almacenar las copias de seguridad en nuestro servidor sin interferir con el volumen principal del sistema, es necesario añadir un disco físico o virtual secundario dedicado exclusivamente a alojar los respaldos.

En este primer paso del laboratorio, prepararemos la máquina virtual de Windows Server 2022 añadiendo una nueva unidad de disco de 50 GB y la inicializaremos dentro del sistema operativo.

### 1.1. Añadir la unidad de disco a la máquina virtual

1. Apaga la máquina virtual Windows Server 2022 (o utiliza una máquina clonada previamente).
2. Accede a la configuración de la máquina virtual en tu hipervisor (ej. VirtualBox).
3. En el apartado de **Almacenamiento**, añade un nuevo disco duro virtual al controlador SATA:
    - **Tipo de formato**: Normal (VDI).
    - **Tamaño virtual**: 50,00 GB.
>
> <img width="958" height="560" alt="image" src="https://github.com/user-attachments/assets/efd6f34f-06fc-4491-ad55-a13a59493896" />

### 1.2. Inicialización del disco en MBR

1. En Windows Server 2022, abre la consola de **Administración de discos** (puedes ejecutar `diskmgmt.msc` desde la ventana Ejecutar o buscarlo en las herramientas del sistema).
2. Al abrir la herramienta, el sistema detectará automáticamente la nueva unidad no inicializada (Disco 1) y mostrará la ventana emergente **Inicializar disco**.
3. Selecciona **Disco 1** y elige el estilo de partición **MBR (Master Boot Record)**.
4. Haz clic en **Aceptar**.
>
> <img width="871" height="662" alt="image" src="https://github.com/user-attachments/assets/9e1077fa-4263-47aa-845d-02a66c82e329" />

### 1.3. Creación de la partición primaria NTFS

1. En la consola de Administración de discos, localiza el **Disco 1**, que mostrará 50,00 GB como espacio no asignado (*Unallocated*).
2. Haz clic derecho sobre el espacio no asignado del Disco 1 y selecciona **Nuevo volumen simple...**.
3. Avanza por el asistente manteniendo el tamaño máximo asignado (50 GB).
4. Asigna una letra de unidad disponible (por ejemplo, `E:`).
5. En las opciones de formato:
    - **Sistema de archivos**: NTFS.
    - **Etiqueta del volumen**: Asigna un nombre descriptivo (por ejemplo, `Disco 1` o `Backup_Drive`).
    - Selecciona la casilla **Dar formato rápido**.
6. Finaliza el asistente. El disco quedará listo como una partición primaria NTFS y accesible desde el Explorador de archivos.
>
> <img width="869" height="662" alt="image" src="https://github.com/user-attachments/assets/7fa2267f-0a81-4fe2-b0f1-b6d7363a1c6a" />

---

## Paso 2. Instalación de componentes (Windows Server Backup)

Una vez preparado el almacenamiento secundario, el siguiente paso consiste en instalar la característica nativa de Windows Server destinada a la gestión de copias de seguridad: **Windows Server Backup**. Por defecto, este componente no se encuentra habilitado en el sistema operativo, por lo que debemos proceder a su instalación e integración dentro de la consola del servidor.

A continuación, realizaremos el proceso de verificación e instalación mediante la consola de automatización **Windows PowerShell**.

### 2.1. Acceso a la consola de PowerShell

1. Haz clic en el menú **Inicio** o utiliza la barra de búsqueda del sistema.
2. Localiza la aplicación **Windows PowerShell** (o **Símbolo del sistema / Windows PowerShell**).
3. Haz clic derecho sobre ella y selecciona la opción **Ejecutar como administrador** para garantizar los permisos necesarios durante la instalación.

### 2.2. Verificación del estado de la característica

Antes de proceder con la instalación, es conveniente comprobar el estado actual del componente en el sistema.

1. En la ventana de PowerShell, introduce el siguiente comando:

    ``Get-WindowsFeature -name *backup*``

2. Presiona `Enter`. La consola mostrará el listado con la característica `Windows-Server-Backup`. 
3. Observa la columna **Install State**; si el componente no ha sido instalado previamente, indicará el estado `Available`.
>
> <img width="869" height="662" alt="image" src="https://github.com/user-attachments/assets/6bab6a85-0ecd-4f95-8ec1-4c43df8e54d7" />

### 2.3. Instalación de Windows Server Backup y herramientas de administración

Para instalar el motor de backups junto con sus consolas gráficas de gestión, ejecutaremos el comando de instalación incluyendo los componentes de administración.

1. Introduce el siguiente comando en la consola de PowerShell:

    ``Install-WindowsFeature -Name Windows-Server-Backup -IncludeManagementTools``

2. Presiona `Enter` y espera a que la barra de progreso complete la instalación.
3. Una vez finalizado el proceso, la consola mostrará la tabla de resultados confirmando el éxito de la operación con la columna **Success** en `True`.
>
> <img width="870" height="664" alt="image" src="https://github.com/user-attachments/assets/9b1d1151-d20a-4cdc-83cd-0bcffc975aba" />

### 2.4. Comprobación final y apertura de la herramienta

1. Para verificar que la instalación se ha completado correctamente, vuelve a ejecutar el comando de comprobación:

    ``Get-WindowsFeature -name *backup*``
>
> <img width="869" height="662" alt="image" src="https://github.com/user-attachments/assets/bb56635f-e898-42c8-8f53-647689023e4b" />

   El estado en la columna **Install State** habrá cambiado a `Installed`.

2. Abre la consola de administración del servidor (**Server Manager**).
3. En el menú superior derecho, haz clic en **Tools** (Herramientas) y selecciona **Windows Server Backup** (Copias de Seguridad de Windows Server).
4. Se abrirá la interfaz gráfica de la herramienta, quedando lista para empezar a programar y realizar copias de seguridad.
>
> <img width="871" height="662" alt="image" src="https://github.com/user-attachments/assets/e629b4ac-80c1-4048-a9c7-0583c77ff937" />

---

## Paso 3. Creación de datos de prueba

Para comprobar el correcto funcionamiento de los procesos de copia de seguridad y posterior restauración, es necesario disponer de un conjunto de datos controlado dentro del volumen principal del sistema (`C:`).

En este paso generaremos una estructura de carpetas y archivos de texto que nos servirá como escenario para validar la recuperación selectiva de información.

### 3.1. Creación de la carpeta principal de respaldo

1. Abre el **Explorador de archivos** de Windows Server 2022.
2. Navega hasta la raíz de la unidad del sistema: `C:\`.
3. Crea una nueva carpeta en la raíz utilizando tu apellido con la nomenclatura requerida:
    - **Nombre de la carpeta**: `TunombreBackup` (por ejemplo, `AdriaBackup`).
>
> <img width="870" height="662" alt="image" src="https://github.com/user-attachments/assets/89cb592a-5cd7-4925-886b-6d4af98d9f6d" />

### 3.2. Estructuración de subcarpetas

Accede a la carpeta recien creada (`C:\TunombreBackup`) y crea tres subcarpetas vacías para simular una jerarquía de archivos:

- `Carpeta1`
- `Carpeta2`
- `Carpeta3`

### 3.3. Creación y edición de ficheros de prueba

Dentro de la raíz de la carpeta `C:\TunombreBackup`, crea tres ficheros de texto plano (`.txt`) e introduce en cada uno de ellos la cadena de texto correspondiente según la siguiente especificación:

1. **Primer archivo**:
    - **Nombre**: `TuNombreTuapellido.txt` (ej. `AdriaMestres.txt`).
    - **Contenido del archivo**: Escribe `TuNombreTuapellido` (ej. `AdriaMestres`).

2. **Segundo archivo**:
    - **Nombre**: `Tuapelllidotunombre.txt` (ej. `MestresAdria.txt`).
    - **Contenido del archivo**: Escribe `Tuapelllidotunombre` (ej. `MestresAdria`).

3. **Tercer archivo**:
    - **Nombre**: `TuCiudad.txt` (ej. `Barcelona.txt`).
    - **Contenido del archivo**: Escribe `TuCiudad` (ej. `Barcelona`).

### 3.4. Verificación del entorno de prueba

Antes de continuar con la ejecución del backup, asegúrate de que en el directorio `C:\TunombreBackup` se encuentran presentes:

- 3 carpetas (`Carpeta1`, `Carpeta2`, `Carpeta3`).
- 3 archivos de texto (`TuNombreTuapellido.txt`, `Tuapelllidotunombre.txt`, `TuCiudad.txt`) con sus contenidos editados y guardados correctamente.
>
> <img width="870" height="662" alt="image" src="https://github.com/user-attachments/assets/1bf98321-3401-4391-9ae7-0a82be7c17f3" />
> <img width="869" height="662" alt="image" src="https://github.com/user-attachments/assets/2f1dd208-9f5d-49bc-b177-cd256f5fb1d0" />
> <img width="870" height="662" alt="image" src="https://github.com/user-attachments/assets/051c8d0e-008e-49b1-817b-19e53b2e642b" />

---

## Paso 4. Copia de seguridad puntual (Backup Once)

Con los datos de prueba creados y la herramienta instalada, realizaremos una copia de seguridad puntual de la carpeta `TunombreBackup`. Esta función permite generar un respaldo ad-hoc de elementos específicos sin necesidad de configurar una programación periódica.

A continuación, ejecutaremos el asistente de copia de seguridad puntual guardando el resultado en la unidad dedicada que preparamos previamente.

### 4.1. Inicio del asistente de copia de seguridad puntual

1. Abre la herramienta **Windows Server Backup** desde el **Server Manager -> Tools**.
2. En la consola principal, en el panel de acciones de la derecha, haz clic en **Backup Once...** (Hacer copia de seguridad una vez...).
3. Se abrirá el asistente emergente **Backup Once Wizard**.
>
> <img width="869" height="664" alt="image" src="https://github.com/user-attachments/assets/e8201772-fd5b-4b88-9069-c76bb93b9561" />

### 4.2. Selección de las opciones de configuración

1. En la pantalla **Backup Options** (Opciones de copia de seguridad), selecciona la opción **Different options** (Opciones diferentes), ya que no disponemos de una copia programada previa.
2. Haz clic en **Next** (Siguiente).
3. En la pantalla **Select Backup Configuration** (Seleccionar configuración de copia de seguridad), marca la opción **Custom** (Personalizada) para seleccionar únicamente la carpeta deseada.
4. Haz clic en **Next**.
>
> <img width="871" height="664" alt="image" src="https://github.com/user-attachments/assets/ab985575-1ee3-4e77-917f-6810312c4857" />

### 4.3. Selección de elementos para la copia de seguridad

1. En la ventana **Select Items for Backup** (Seleccionar elementos para la copia de seguridad), haz clic en el botón **Add Items** (Agregar elementos).
2. En el árbol de navegación, despliega la unidad local `C:` y marca la casilla correspondiente a la carpeta de datos de prueba (`TunombreBackup`, por ejemplo `AdriaBackup`).
3. Haz clic en **OK**.
4. Verifica que la carpeta aparece en la lista de elementos a respaldar y haz clic en **Next**.
>
> <img width="869" height="662" alt="image" src="https://github.com/user-attachments/assets/a19ecd74-355f-4178-96e4-532e8ebeb8fd" />

### 4.4. Selección del destino del respaldo

1. En la pantalla **Specify Destination Type** (Especificar tipo de destino), selecciona la opción **Local drives** (Unidades locales).
2. Haz clic en **Next**.
3. En la ventana **Select Backup Destination** (Seleccionar destino de copia de seguridad), despliega la lista y selecciona el disco secundario de 50 GB que preparamos en el punto 1 (por ejemplo, `Backup_Drive (E:)`).
4. Haz clic en **Next**.
>
> <img width="869" height="662" alt="image" src="https://github.com/user-attachments/assets/2be43573-7043-4f52-ac6b-e523eb8bb972" />
> <img width="870" height="664" alt="image" src="https://github.com/user-attachments/assets/a3e38df2-b28d-4961-8301-839f08105ea3" />

### 4.5. Confirmación y ejecución del proceso

1. En la pantalla **Confirmation** (Confirmación), revisa el resumen del respaldo:
    - **File excluded**: None.
    - **Backup destination**: Backup_Drive (E:).
    - **Backup items**: C:\TunombreBackup.
2. Haz clic en el botón **Backup** para iniciar el proceso.
3. Se mostrará la pantalla **Backup Progress** mostrando la barra de progreso de la transferencia de datos.
4. Una vez completado el estado pasará a **Completed** (Completado). Haz clic en **Close** para cerrar el asistente.
>
> <img width="870" height="661" alt="image" src="https://github.com/user-attachments/assets/58cf87eb-3985-47be-b865-3358920c63ec" />


---

## Paso 5. Restauración de datos (Recovery)

Una vez generada la copia de seguridad puntual, simularemos un escenario real de pérdida de información borrando los datos originales para posteriormente recuperarlos mediante el asistente de restauración de Windows Server Backup.

### 5.1. Simulación de pérdida de datos

1. Abre el **Explorador de archivos** y navega hasta la ruta `C:\`.
2. Selecciona la carpeta `TunombreBackup` (por ejemplo, `AdriaBackup`) que contiene la estructura de pruebas.
3. Elimina completamente la carpeta y su contenido. Asegúrate de vaciar la **Papelera de reciclaje** para confirmar la pérdida total de los archivos en el volumen original.

### 5.2. Inicio del asistente de recuperación

1. Abre la herramienta **Windows Server Backup** desde **Server Manager -> Tools**.
2. En el panel de acciones lateral derecho, haz clic en **Recover...** (Recuperar...).
3. Se abrirá el asistente emergente **Recovery Wizard**.

### 5.3. Selección de la ubicación del backup y fecha

1. En la pantalla **Getting Started** (Inicio), selecciona la opción **This server** (Este servidor), ya que la copia de seguridad se encuentra almacenada en el disco secundario de la propia máquina.
2. Haz clic en **Next** (Siguiente).
3. En la ventana **Select Backup Date** (Seleccionar fecha de copia de seguridad):
    - Revisa el calendario y selecciona el día en el que se realizó la copia puntual (las fechas con copias disponibles aparecerán resaltadas en negrita).
    - En el menú desplegable de la derecha (**Time**), selecciona la hora exacta del respaldo realizado.
4. Haz clic en **Next**.
>
> <img width="872" height="662" alt="image" src="https://github.com/user-attachments/assets/61ccb3a4-365d-4716-bda9-5c39d989eb13" />
> <img width="870" height="664" alt="image" src="https://github.com/user-attachments/assets/06b4a483-055e-463b-91e7-b5e89e654a92" />

### 5.4. Selección del tipo de recuperación

1. En la pantalla **Select Recovery Type** (Seleccionar tipo de recuperación), selecciona la opción **Files and folders** (Archivos y carpetas) para realizar una restauración granular de elementos específicos.
2. Haz clic en **Next**.
>
> <img width="869" height="661" alt="image" src="https://github.com/user-attachments/assets/b411b23a-de2a-4ec3-83ea-5b134a7cb07c" />

### 5.5. Selección de elementos a restaurar

1. En la ventana **Select Items to Recover** (Seleccionar elementos a recuperar), despliega el árbol de navegación del panel izquierdo (`SRVAdria -> Local disk (C:)`).
2. Selecciona la carpeta `TunombreBackup` (o marca individualmente las subcarpetas `Carpeta1`, `Carpeta2`, `Carpeta3` y los 3 archivos `.txt` en el panel derecho).
3. Haz clic en **Next**.
>
> <img width="869" height="663" alt="image" src="https://github.com/user-attachments/assets/0abc643e-8989-4152-9f12-04d509337e83" />

### 5.6. Configuración de opciones de restauración

1. En la pantalla **Specify Recovery Options** (Especificar opciones de recuperación), mantén las opciones predeterminadas:
    - **Recovery destination**: Original location (Ubicación original).
    - **Existing items**: Create copies so that you have both versions (o *Overwrite the existing ones*).
    - **Security settings**: Restore the Access Control List (ACL) permissions.
2. Haz clic en **Next**.
>
> <img width="870" height="662" alt="image" src="https://github.com/user-attachments/assets/0a9ba7f6-2e88-461e-8a55-5ff25fd6123a" />

### 5.7. Confirmación y verificación de los datos restaurados

1. En la ventana **Confirmation** (Confirmación), revisa el resumen de los elementos a recuperar y haz clic en **Recover**.
2. Observa la ventana **Recovery Progress** hasta que el estado cambie a **Completed**.
3. Haz clic en **Close** para cerrar el asistente.
4. Abre el **Explorador de archivos**, navega hasta `C:\AdriaBackup` y comprueba que se han restaurado correctamente la totalidad de las carpetas y los tres ficheros `.txt` con sus contenidos intactos.
>
> <img width="872" height="663" alt="image" src="https://github.com/user-attachments/assets/b8c86429-dac6-43b5-8cf9-c4910c28700a" />

---

## Paso 6. Planificación automatizada (Backup Schedule)

Para garantizar la protección continua del servidor ante posibles fallos del sistema o desastres, no es suficiente con realizar copias puntuales manuales. Es fundamental configurar copias de seguridad automáticas y programadas.

En este apartado configuraremos la herramienta **Windows Server Backup** para realizar una copia completa de todo el sistema (*Full server*) de forma programada diariamente en una unidad de disco dedicada.

### 6.1. Inicio del asistente de programación

1. Abre la consola **Windows Server Backup** desde **Server Manager -> Tools**.
2. En el panel de acciones lateral derecho, haz clic en **Backup Schedule...** (Programación de copia de seguridad...).
3. Se abrirá el asistente emergente **Backup Schedule Wizard**. Haz clic en **Next** (Siguiente) en la pantalla inicial.

### 6.2. Selección del tipo de configuración de respaldo

1. En la pantalla **Select Backup Configuration** (Seleccionar configuración de copia de seguridad), selecciona la opción **Full server (recommended)** (Servidor completo).
2. Esta opción garantizará la copia de seguridad de todos los datos del servidor, aplicaciones, estado del sistema y la posibilidad de realizar una recuperación ante desastres (*Bare Metal Recovery*).
3. Haz clic en **Next**.

### 6.3. Especificación del horario de ejecución

1. En la ventana **Specify Backup Time** (Especificar hora de copia de seguridad), selecciona la opción **Once a day** (Una vez al día).
2. En el menú desplegable de horas, selecciona **3:00 AM** (o **3:00 H** del sistema).
3. Haz clic en **Next**.

### 6.4. Especificación del tipo de destino

1. En la pantalla **Specify Destination Type** (Especificar tipo de destino), selecciona la opción **Back up to a hard disk that is dedicated for backups (recommended)** (Hacer copia de seguridad en un disco duro dedicado a copias).
2. Esta opción formateará la unidad destino seleccionada para uso exclusivo del motor de respaldos, mejorando el rendimiento global.
3. Haz clic en **Next**.

### 6.5. Selección del disco de destino

1. En la ventana **Select Destination Disk** (Seleccionar disco de destino), haz clic en el botón **Show All Available Disks...** (Mostrar todos los discos disponibles...).
2. En la ventana emergente, marca la casilla correspondiente al disco secundario de 50 GB que preparamos previamente y pulsa **OK**.
3. De vuelta en la ventana principal, marca la casilla del disco seleccionado y haz clic en **Next**.
4. Aparecerá una ventana de advertencia de Windows Server Backup indicando que el disco seleccionado será formateado y las particiones existentes se perderán. Haz clic en **Yes** para confirmar.

### 6.6. Confirmación y finalización del programador

1. En la ventana **Confirmation** (Confirmación), verifica los datos de la programación:
    - **Schedule**: Daily at 3:00 AM.
    - **Advanced options**: VSS Full Backup.
    - **Destination**: Disco dedicado seleccionado.
2. Haz clic en **Finish** para guardar la programación.
3. El sistema procederá a formatear la unidad e inscribir la directiva. Una vez completado, haz clic en **Close** para cerrar el asistente.
