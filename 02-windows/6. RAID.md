# Implantación y Gestión de Sistemas RAID en Windows Server 2022

En el ámbito de la administración de sistemas informáticos, la disponibilidad, el rendimiento y la integridad de los datos son pilares fundamentales para garantizar la continuidad del negocio. A medida que las organizaciones crecen, el volumen de información y la criticidad de las operaciones exigen infraestructuras capaces de tolerar fallos de hardware sin interrumpir el servicio. 

En este laboratorio práctico, abordaremos la configuración y gestión del almacenamiento redundante dentro del entorno de Windows Server 2022. A través de un enfoque paso a paso, simularé el despliegue de una máquina virtual en un entorno de pruebas donde configuraremos diferentes arreglos de almacenamiento y evaluaremos la respuesta del sistema ante fallos de discos físicos.

---

## ¿Qué es un RAID y para qué sirve?

**RAID** es el acrónimo de *Redundant Array of Independent Disks* (Matriz Redundante de Discos Independientes). Consiste en una tecnología que combina múltiples unidades de almacenamiento físico (discos duros o SSDs) para que el sistema operativo las reconozca como un único volumen lógico o unidad de almacenamiento.

### ¿Para qué sirve?

Dependiendo del tipo de configuración o nivel de RAID seleccionado, este sistema permite alcanzar distintos objetivos técnicos:

1. **Incremento del rendimiento (Velocidad):** Al distribuir la lectura y escritura de datos entre varios discos en paralelo, se aceleran las operaciones de entrada/salida (I/O).
2. **Tolerancia a fallos y redundancia (Seguridad):** Permite mantener la disponibilidad de los datos aunque uno o más discos fallen, mediante el duplicado en espejo o la generación de bloques de paridad.
3. **Optimización y consolidación del espacio:** Facilita la creación de grandes volúmenes de almacenamiento combinando la capacidad de varios discos físicos.

Existen soluciones RAID basadas en hardware (controladoras dedicadas) y basadas en software. En esta guía nos centraremos en el **RAID por software** que proporciona de forma nativa Windows Server 2022 a través del Administrador de Discos (Dynamic Disks).

## Tipos de RAID (Niveles de RAID)

Existen diferentes tipos o niveles de RAID, cada uno diseñado para equilibrar de distinta forma el rendimiento, la capacidad de almacenamiento y la seguridad de los datos:

- **RAID 0 (Volumen distribuido en bandas / Striped):** 
  Distribuye los datos equitativamente entre dos o más discos sin incluir información de paridad ni redundancia. 
  - *Ventaja:* Ofrece el máximo rendimiento en lectura/escritura y aprovecha el 100% de la capacidad de los discos.
  - *Desventaja:* No tiene tolerancia a fallos. Si un solo disco falla, se pierden todos los datos del volumen.

- **Volumen Distribuido (Spanned Volume):** 
  Suma el espacio de varios discos físicos para crear un único volumen lógico. A diferencia de RAID 0, no escribe en bandas paralelas sino secuencialmente.
  - *Ventaja:* Permite combinar discos de diferentes tamaños sin desperdiciar espacio y extender volúmenes dinámicamente.
  - *Desventaja:* Tampoco ofrece redundancia ni tolerancia a fallos.

- **RAID 1 (Espejo / Mirrored):** 
  Duplica exactamente los mismos datos en dos discos físicos de forma simultánea.
  - *Ventaja:* Alta tolerancia a fallos. Si uno de los discos se avería, el sistema sigue funcionando con el disco restante sin interrupción ni pérdida de datos.
  - *Desventaja:* Reduce la capacidad útil al 50% del total de almacenamiento disponible.

- **RAID 5 (Distribuido con paridad):** 
  Requiere un mínimo de 3 discos. Distribuye tanto los datos como los bloques de información de paridad a lo largo de todas las unidades del conjunto.
  - *Ventaja:* Ofrece un excelente equilibrio entre alto rendimiento, tolerancia al fallo de 1 disco y eficiencia de espacio (se pierde únicamente la capacidad equivalente a 1 disco para la paridad).
  - *Desventaja:* La velocidad de escritura se ve ligeramente penalizada por el cálculo continuo de los bloques de paridad.

<img width="1920" height="1080" alt="RAIDS" src="https://github.com/user-attachments/assets/86f737da-fb35-47c7-bc9e-74b5716d2ddd" />

---

## Objetivos del Laboratorio

A lo largo de este documento y mediante los casos prácticos guiados paso a paso, se cumplirán los siguientes objetivos de aprendizaje y configuración técnica:

- **Despliegue del entorno base:** Configurar una máquina virtual con Windows Server 2022 e incorporar un total de 10 discos virtuales adicionales con distintas capacidades para las prácticas.
- **Implementación de RAID 0 (Striped Volume):** Crear un volumen distribuido en bandas con 2 discos de 30 GB (acotando la partición a 8 GB por disco) para maximizar la velocidad de acceso.
- **Configuración de Volumen Distribuido (Spanned Volume):** Crear un volumen extendido combinando el espacio libre disponible en discos y expandirlo agregando un disco de 16 GB completo.
- **Análisis comparativo:** Comprender y documentar las diferencias clave en rendimiento, flexibilidad y tolerancia a fallos entre RAID 0 y un Volumen Distribuido.
- **Implementación de RAID 1 (Mirrored Volume):** Configurar un volumen en espejo con 2 discos de 6 GB para garantizar la tolerancia a fallos mediante la duplicación exacta de datos.
- **Implementación de RAID 5:** Crear un volumen redundante con paridad distribuida empleando 3 discos de 8 GB, analizando la capacidad real útil frente al espacio reservado para la paridad.
- **Simulación y recuperación ante fallos en RAID 1:** Simular una avería forzando el estado offline de un disco, verificar el acceso continuo a los datos y reconstruir el conjunto reemplazando la unidad averiada por un nuevo disco espejo.
- **Simulación y recuperación ante fallos en RAID 5:** Forzar la caída de un disco del conjunto RAID 5, comprobar la continuidad del servicio sin pérdida de información y reparar el volumen utilizando la función "Repair Volume" con un disco de repuesto.

---

## Parte 1. Despliegue del entorno base

Para llevar a cabo la implantación y configuración de los sistemas redundantes y tolerantes a errores (RAID) en Windows Server 2022, el primer paso consiste en preparar la infraestructura virtual base sobre la cual realizaremos todas las operaciones en la consola de administración de discos.

### Preparación de la Máquina Virtual

1. **Disponer de la máquina virtual:** Partimos de un equipo virtual con Windows Server 2022 (puedes realizar un clon de la máquina virtual utilizada anteriormente). La máquina debe encontrarse totalmente apagada antes de modificar su configuración de almacenamiento físico.
2. **Adición de discos duros virtuales:** Añadiremos un total de 10 discos adicionales a la máquina virtual con las capacidades específicas requeridas para cada tipo de RAID:
    * 2 discos de 30 GB cada uno (destinados al RAID 0).
    * 1 disco de 16 GB (destinado al volumen distribuido / spanned volume).
    * 3 discos de 6 GB cada uno (destinados al RAID 1 / mirroring).
    * 4 discos de 8 GB cada uno (destinados al RAID 5).
3. **Verificación de almacenamiento:** Los discos añadidos se mostrarán en la configuración del hipervisor con sus respectivas rutas y tamaños (por ejemplo, desde `Windows Server Lab CLON_1.vdi` de 30,00 GB hasta `Windows Server Lab CLON_10.vdi` de 8,00 GB).
>
> <img width="959" height="802" alt="image" src="https://github.com/user-attachments/assets/875c3591-0e31-499a-bd8d-ef4f68ef28d9" />
> <img width="959" height="559" alt="image" src="https://github.com/user-attachments/assets/28e658b9-73eb-435f-9305-009624fd00d2" />

### Creación de una Instantánea (Snapshot) Inicial

Una vez agregados todos los discos virtuales necesarios en el sistema, es una buena práctica de administración realizar una copia de seguridad del estado actual:

1. Accede al gestor de la máquina virtual y selecciona la opción de crear una instantánea (*snapshot*).
2. Asigna un nombre identificativo a la instantánea, por ejemplo: `Instantánea 1`.
3. Introduce una descripción clara, como `Server con discos agregados`, para asegurar que disponemos de un punto de restauración limpio antes de formatear o configurar los volúmenes dinámicos.
>
> <img width="958" height="560" alt="image" src="https://github.com/user-attachments/assets/c5895fc0-a4c7-4da7-866b-04032542b1c3" />

---

## Parte 2. Implementación de RAID 0 (Striped Volume)

En este apartado procederemos a configurar un volumen seccionado (RAID 0) utilizando el Administrador de discos de Windows Server. Para ello, emplearemos los dos discos de 30 GB añadidos previamente en la fase de preparación, pero acotando la partición a únicamente 8 GB por disco para el arreglo RAID.

### Explicación del paso a paso

1. **Acceso al Administrador de discos:**
   * Iniciamos el equipo Windows Server y abrimos el **Administrador de discos** (`diskmgmt.msc`).

2. **Inicialización de los discos:**
   * Al inicializarse el Administrador de discos, el sistema detectará automáticamente los nuevos discos instalados y nos solicitará inicializarlos.
   * Seleccionamos los discos que vayamos a utilizar y elegimos el estilo de partición adecuado. En este caso, seleccionamos todos los discos e inicializamos con el esquema **MBR (Master Boot Record)**.
   * Comprobamos que todos los discos necesarios se encuentren en estado **Habilitado (Online)**. Si alguno figura como *Offline*, hacemos clic izquierdo/derecho sobre él, seleccionamos la opción **Online** y posteriormente lo inicializamos en MBR o GPT.
>
> <img width="872" height="664" alt="image" src="https://github.com/user-attachments/assets/5b8ce04c-180d-424a-b3f3-feb0c5b571d9" />

3. **Creación del volumen seccionado:**
   * Hacemos clic izquierdo/derecho en la parte sin asignar de uno de los dos discos de 30 GB que vamos a emplear (Disco 1 o Disco 2) y seleccionamos la opción **New Striped Volume...** (Nuevo volumen seccionado).
   * En la pantalla de bienvenida del asistente (**Welcome to the New Striped Volume Wizard**), pulsamos en **Next** para continuar.

4. **Selección de discos y tamaño:**
   * En la ventana **Select Disks**, elegimos los discos de 30 GB de la lista de disponibles (*Available*) y los agregamos a la lista de seleccionados (*Selected*) mediante el botón **Add >**. Deben quedar seleccionados ambos discos de 30 GB.
   * En el campo **Select the amount of space in MB:**, especificamos el espacio que aportará cada disco al RAID 0. Indicamos `8192` MB (equivalente a 8 GB por disco) y pulsamos en **Next**.
>
> <img width="870" height="664" alt="image" src="https://github.com/user-attachments/assets/3d11ce32-4ccd-48f9-b888-adad57a69b6c" />

5. **Asignación de letra de unidad:**
   * En la pestaña **Assign Drive Letter or Path**, seleccionamos **Assign the following drive letter:** y le asignamos la letra **E:** al volumen RAID 0. Pulsamos en **Next**.

6. **Formato del volumen:**
   * En la ventana **Format Volume**, elegimos la opción **Format this volume with the following settings**:
     * **File system:** NTFS
     * **Allocation unit size:** Default
     * **Volume label:** RAID 0
   * Marcamos la casilla **Perform a quick format** (Realizar un formato rápido) y pulsamos en **Next**.
>
> <img width="871" height="662" alt="image" src="https://github.com/user-attachments/assets/fe2e1130-12ff-4046-8957-1fcad071f5ea" />

7. **Finalización del asistente y conversión a disco dinámico:**
   * Verificamos todas las configuraciones seleccionadas en el resumen final del asistente y pulsamos en **Finish**.
   * A continuación, aparecerá una ventana emergente de advertencia del Administrador de discos que nos informa de que los discos seleccionados se van a convertir de discos básicos a **discos dinámicos**. Confirmamos pulsando en **Yes**.
   * Comprobamos que el **RAID 0** queda creado y reflejado correctamente en la consola del Administrador de discos.

### Verificación de escritura en el RAID 0

1. Abrimos el **Explorador de archivos** de Windows Server y accedemos a la nueva unidad lógica **RAID 0 (E:)**.
2. Comprobamos la capacidad del volumen (aproximadamente 15.9 GB de espacio libre) y creamos una carpeta de prueba dentro de la unidad (por ejemplo, llamada `Prueba Raid 0`) para validar que el almacenamiento es plenamente funcional para operaciones de lectura y escritura.
>
> <img width="872" height="662" alt="image" src="https://github.com/user-attachments/assets/8bd097d0-774c-41f7-bc82-ddf3fecfd66d" />
> <img width="870" height="662" alt="image" src="https://github.com/user-attachments/assets/b8681106-63bb-405b-82af-9281551f381f" />
> <img width="869" height="664" alt="image" src="https://github.com/user-attachments/assets/48199eb8-068e-44e5-b75e-8bf7f3f19aca" />

---

## Parte 3. Configuración de Volumen Distribuido (Spanned Volume) y Análisis Comparativo

En esta sección procederemos a crear un volumen distribuido (*spanned volume*) aprovechando el espacio libre sin asignar que ha quedado en los dos discos de 30 GB tras crear la partición del RAID 0. Posteriormente, expandiremos dicho volumen añadiendo un disco físico completo de 16 GB y analizaremos las diferencias funcionales entre el RAID 0 y un volumen distribuido.

### Explicación del paso a paso: Creación del Volumen Distribuido

1. **Inicio del asistente de volumen distribuido:**
   * Dentro del **Administrador de discos**, localizamos la partición vacía o espacio sin asignar (*Unallocated*) del primer disco de 30 GB (Disco 1).
   * Hacemos clic derecho sobre dicho espacio sin asignar y seleccionamos la opción **New Spanned Volume...** (Nuevo volumen distribuido).

2. **Selección de discos y capacidad:**
   * En la ventana **Select Disks** del asistente, seleccionamos tanto el **Disco 1** como el **Disco 2** de la lista de discos disponibles y los agregamos con el botón **Add >**.
   * Dejamos seleccionada la capacidad máxima disponible de cada disco para conformar el volumen distribuido y pulsamos en **Next**.
>
> <img width="870" height="663" alt="image" src="https://github.com/user-attachments/assets/311dbddd-146b-44b2-a190-2f3c372c47e8" />

3. **Asignación de letra y formato:**
   * En la sección **Assign Drive Letter or Path**, le asignamos la letra de unidad **F:** y pulsamos en **Next**.
   * En la ventana **Format Volume**, formateamos el volumen con las siguientes opciones:
     * **File system:** NTFS
     * **Allocation unit size:** Default
     * **Volume label:** DISTRIBUIDO
   * Marcamos la casilla **Perform a quick format** y pulsamos en **Next** para completar el asistente.
>
> <img width="870" height="662" alt="image" src="https://github.com/user-attachments/assets/2e1b9764-bb2d-4265-b12b-93ecb1973ca9" />
> <img width="869" height="664" alt="image" src="https://github.com/user-attachments/assets/9f8b391c-9b13-4d99-b3dc-46cb760c850d" />

### Extensión del Volumen Distribuido con un Disco Completo (16 GB)

Una de las características principales del volumen distribuido es la posibilidad de ampliar su espacio de almacenamiento agregando nuevas unidades físicas sobre la marcha.

1. **Iniciar extensión:**
   * En el **Administrador de discos**, hacemos clic derecho sobre cualquiera de las particiones pertenecientes al volumen **DISTRIBUIDO (F:)** y seleccionamos **Extend Volume...** (Extender volumen).
   * Aparecerá la ventana de bienvenida; pulsamos en **Next** para continuar.

2. **Selección del nuevo disco de 16 GB:**
   * En la pantalla **Select Disks**, elegimos el disco de 16 GB que añadimos previamente en el entorno base (Disco 3) de la lista de disponibles y lo agregamos con **Add >**.
   * Nos aseguramos de asignar la totalidad de su espacio y pulsamos en **Next** y luego en **Finish**.
>
> <img width="869" height="664" alt="image" src="https://github.com/user-attachments/assets/6f351c87-5d1d-4e47-b68c-9c59a831c94f" />

3. **Confirmación y verificación:**
   * El Administrador de discos nos mostrará una ventana emergente avisando de que el disco de 16 GB pasará de disco básico a **disco dinámico**. Pulsamos en **Yes** para confirmar.
   * Comprobamos en la consola que el volumen **DISTRIBUIDO (F:)** abarca ahora los tres discos y muestra la suma total de sus capacidades.

4. **Comprobación de escritura:**
   * Abrimos el **Explorador de archivos**, accedemos a la unidad **DISTRIBUIDO (F:)** y creamos una carpeta de prueba (por ejemplo, `Prueba Vol. Distribuido`) para certificar que el volumen funciona correctamente.
>
> <img width="869" height="661" alt="image" src="https://github.com/user-attachments/assets/7d94aaba-f207-4e2b-ac9e-8903660241a9" />
> <img width="871" height="664" alt="image" src="https://github.com/user-attachments/assets/5f593784-41c8-4df4-a34e-73d9a90eac5b" />

### ¿Qué diferencia hay entre RAID 0 y un volumen distribuido (spanned volume)?

Aunque ambos métodos permiten combinar múltiples discos físicos para presentar una única unidad lógica de mayor tamaño, existen diferencias fundamentales en su arquitectura y comportamiento:

* **Estructura y fraccionamiento de datos:** 
  * Un **RAID 0** divide la información en bloques fijos de datos distribuidos en bandas (*stripes*) a lo largo de todos los discos simultáneamente.
  * Un **volumen distribuido** no fragmenta los datos en bandas; simplemente enlaza y suma las particiones de varios discos de forma secuencial, simulando un único volumen tradicional.

* **Rendimiento:** 
  * El **RAID 0** ofrece un rendimiento significativamente mayor en operaciones de lectura y escritura, ya que el sistema operativo puede leer y escribir de forma paralela en todos los discos del conjunto a la vez.
  * El **volumen distribuido** no ofrece esta mejora de velocidad paralela, manteniendo un rendimiento similar al de un disco individual.

* **Flexibilidad y combinación de discos:** 
  * El **RAID 0** está limitado por la capacidad del disco más pequeño (las bandas se dividen en partes iguales). Por ejemplo, en un RAID 0 con un disco de 500 GB y otro de 300 GB, el RAID usará únicamente 300 GB de cada disco (600 GB totales).
  * El **volumen distribuido** permite combinar discos de capacidades y tamaños completamente diferentes sin desperdiciar espacio, e incluso permite agregar o retirar espacio dinámicamente sin necesidad de reconfigurar o formatear el volumen completo.
 
---

## 4. Implementación de RAID 1 (Mirrored Volume)

En este apartado llevaremos a cabo la configuración de un volumen en espejo (RAID 1) utilizando la consola de administración de discos de Windows Server. Para ello, emplearemos 2 discos de 6 GB de los asignados previamente al entorno base, garantizando la tolerancia a fallos mediante la duplicación exacta de la información en ambos discos.

### Explicación del paso a paso

1. **Selección del tipo de volumen:**
   * En el **Administrador de discos**, localizamos uno de los discos de 6 GB sin asignar (por ejemplo, el Disco 4).
   * Hacemos clic derecho sobre el espacio no asignado y seleccionamos la opción **New Mirrored Volume...** (Nuevo volumen reflejado).

2. **Asistente de configuración:**
   * En la ventana de bienvenida del asistente (**Welcome to the New Mirrored Volume Wizard**), pulsamos en **Next** para continuar.

3. **Selección de discos para el mirroring:**
   * En la ventana **Select Disks**, seleccionamos de la lista de discos disponibles (*Available*) los dos discos de 6 GB que utilizaremos para la redundancia (en este caso, el **Disco 4** y el **Disco 5**).
   * Los añadimos a la lista de seleccionados (*Selected*) mediante el botón **Add >**.
   * Verificamos que el espacio seleccionado sea de `6141` MB (la capacidad total de los discos) y pulsamos en **Next**.
>
> <img width="870" height="661" alt="image" src="https://github.com/user-attachments/assets/910533f3-094a-47e1-b411-4cce7b5fe5c9" />

4. **Asignación de letra y formato del volumen:**
   * En la pantalla **Assign Drive Letter or Path**, le asignamos una letra de unidad disponible, por ejemplo la letra **G:**. Pulsamos en **Next**.
   * En la ventana **Format Volume**, elegimos la opción **Format this volume with the following settings**:
     * **File system:** NTFS
     * **Allocation unit size:** Default
     * **Volume label:** RAID 1
   * Marcamos la casilla **Perform a quick format** para realizar un formateo rápido y pulsamos en **Next**.
>
> <img width="871" height="664" alt="image" src="https://github.com/user-attachments/assets/94e13b22-4f20-4357-8e22-45f52fe9d570" />

5. **Finalización y conversión a disco dinámico:**
   * Verificamos las opciones seleccionadas en la pantalla final del resumen y pulsamos en **Finish**.
   * Aparecerá una ventana emergente de aviso del Administrador de discos indicando que los discos seleccionados se van a convertir en **discos dinámicos**. Confirmamos la operación pulsando en **Yes**.
   * Comprobamos en el Administrador de discos que ambos discos quedan configurados como un único volumen lógico reflejado (**RAID 1 (G:)**).
>
> <img width="870" height="661" alt="image" src="https://github.com/user-attachments/assets/3ef80560-e26d-43c0-bd99-86b463ca8d75" />

### Verificación de escritura en el RAID 1

1. Abrimos el **Explorador de archivos** de Windows Server y accedemos al nuevo volumen **RAID 1 (G:)**.
2. Comprobamos que el espacio disponible equivale al tamaño de un único disco de 6 GB (aproximadamente 5.96 GB libres), ya que el segundo disco actúa de espejo transparente.
3. Creamos una carpeta de prueba dentro del volumen (por ejemplo, llamada `Prueba RAID 1`) para confirmar el correcto funcionamiento de las operaciones de lectura y escritura redundantes.
>
> <img width="869" height="662" alt="image" src="https://github.com/user-attachments/assets/39fcc5ba-3d70-4adb-9fc4-d00dcc122478" />
> <img width="870" height="662" alt="image" src="https://github.com/user-attachments/assets/8f1090ff-e515-45c2-a4c1-438d4998e352" />

--- 

## 5. Creación de un RAID 5 y Análisis de Capacidad

En este apartado configuraremos un volumen RAID 5 utilizando la consola de administración de discos de Windows Server 2022. Para ello emplearemos 3 discos de 8 GB asignados en nuestro entorno de pruebas, ofreciendo tolerancia a fallos mediante la distribución de bloques de paridad entre todas las unidades.

### Explicación del paso a paso

1. **Inicio de la creación del volumen:**
   * En el **Administrador de discos**, localizamos uno de los discos de 8 GB sin asignar (por ejemplo, el Disco 7).
   * Hacemos clic izquierdo/derecho sobre el espacio sin asignar y seleccionamos la opción **RAID-5 Volume...** (Nuevo volumen RAID-5).

2. **Asistente de instalación:**
   * En la pantalla de bienvenida del asistente (**Welcome to the New RAID-5 Volume Wizard**), pulsamos en **Next** para continuar.

3. **Selección de discos:**
   * En la ventana **Select Disks**, elegimos de la lista de disponibles (*Available*) los tres discos de 8 GB que vamos a emplear para el arreglo (por ejemplo, **Disco 7**, **Disco 8** y **Disco 9**).
   * Los añadimos a la lista de seleccionados (*Selected*) mediante el botón **Add >** y pulsamos en **Next**.
>
> <img width="870" height="664" alt="image" src="https://github.com/user-attachments/assets/c91587b9-f98a-4da4-bc05-164e4f3b6fdd" />

4. **Asignación de letra y formato del volumen:**
   * En la pestaña **Assign Drive Letter or Path**, le asignamos una letra de unidad disponible, por ejemplo la letra **H:**, y pulsamos en **Next**.
   * En la ventana **Format Volume**, formateamos el volumen en el sistema de archivos **NTFS**, le asignamos la etiqueta **RAID 5** y marcamos la casilla **Perform a quick format** (Formato rápido). Pulsamos en **Next**.
>
> <img width="869" height="664" alt="image" src="https://github.com/user-attachments/assets/c63e2c36-397d-4ab5-a76c-5fc0cb59c543" />

5. **Finalización y sincronización inicial:**
   * Verificamos las configuraciones en la pantalla de resumen y pulsamos en **Finish**.
   * En la ventana emergente de aviso, confirmamos la conversión de los discos a **discos dinámicos** haciendo clic en **Yes**.
   * Una vez finalizada la configuración, los discos del RAID 5 entrarán en estado **Resynching** (Sincronizando) en el Administrador de discos. Esperamos a que la sincronización se complete al 100%.

6. **Comprobación de escritura:**
   * Abrimos el **Explorador de archivos**, accedemos a la nueva unidad **RAID 5 (H:)** y creamos una carpeta de prueba (por ejemplo, `Prueba RAID 5`) para asegurar la funcionalidad de lectura y escritura del volumen.
> <img width="872" height="662" alt="image" src="https://github.com/user-attachments/assets/238cd47b-5c5f-40c7-b5fe-33efa959d9c0" />
> <img width="871" height="664" alt="image" src="https://github.com/user-attachments/assets/6228e6d4-9faa-4e2f-b0ef-b0a2adb42ccf" />

### ¿Qué espacio en disco tenemos para guardar información con los 3 discos de 8 GB en RAID 5? ¿Cuánto perdemos de espacio en disco por utilizar un RAID 5?

* **Capacidad útil disponible:** Disponemos de **16 GB** de espacio de almacenamiento utilizable para guardar información.
* **Espacio reservado / perdido:** De los 24 GB totales contratados físicamente (3 discos x 8 GB), perdemos **8 GB** de espacio útil (es decir, la capacidad equivalente a uno de los tres discos del conjunto).
* **Motivo:** Este espacio "perdido" no queda inutilizado de forma aleatoria, sino que se emplea para distribuir los **bloques de paridad** de forma alternativa en cada uno de los volúmenes del RAID 5, permitiendo la reconstrucción completa de los datos en caso de que falle cualquiera de las tres unidades.

### Observación general de las unidades lógicas creadas

Al finalizar este proceso, si accedemos al **Explorador de archivos** de Windows Server, podemos observar en el apartado *Devices and drives* todas las unidades lógicas operativas y sus respectivos tamaños:

* **Disco C: (Sistema):** Unidad principal del sistema operativo.
* **RAID 0 (E:):** ~16 GB libres (2 discos x 8 GB en bandas).
* **DISTRIBUIDO (F:):** ~60 GB libres (suma de espacio extendido en múltiples discos).
* **RAID 1 (G:):** ~6 GB libres (2 discos x 6 GB en espejo).
* **RAID 5 (H:):** ~16 GB libres (3 discos x 8 GB con paridad).
>
> <img width="869" height="661" alt="image" src="https://github.com/user-attachments/assets/06a99908-72a5-4f82-b4d2-76504be8c2d8" />

**¡IMPORTANTE!** Realiza una snapshot de la MV para seguir con los siguientes apartados.

---

## 6. Simulación y Recuperación ante Fallos en RAID 1

En este apartado pondremos a prueba la tolerancia a fallos del volumen en espejo (RAID 1) configurado previamente. Para ello, simularemos la avería física de uno de los discos virtuales desactivándolo, verificaremos el acceso ininterrumpido a la información y recuperaremos la funcionalidad del conjunto agregando un disco de repuesto.

### Simulación de la avería del RAID 1

1. Nos dirigimos al **Administrador de discos** y buscamos los volúmenes pertenecientes al **RAID 1 (G:)** (Disco 4 y Disco 5).
2. Hacemos clic derecho sobre el encabezado del **Disco 5** y pulsamos en la opción **Offline** para desactivarlo y simular una desconexión o avería del disco duro.
3. **Comprobación del fallo:** El Administrador de discos reflejará el estado del volumen **RAID 1 (G:)** en color rojo con el aviso **Failed Redundancy** (Redundancia con errores), indicando que se ha perdido la tolerancia a fallos en el volumen.
>
> <img width="869" height="664" alt="image" src="https://github.com/user-attachments/assets/c7d188e2-e14b-4bb3-866c-39f3068a7ada" />
> <img width="871" height="662" alt="image" src="https://github.com/user-attachments/assets/db10346b-232e-48c2-ba14-f75afcb1ff26" />

### Verificación del acceso a la información en estado degradado

1. Abrimos el **Explorador de archivos** y accedemos a la unidad **RAID 1 (G:)**.
2. Comprobamos que el volumen sigue estando plenamente operativo y que los ficheros y carpetas creados antes del fallo (como la carpeta `Prueba RAID 1`) siguen intactos y accesibles.
3. Para demostrar que es posible seguir trabajando sin redundancia, creamos una nueva carpeta de prueba dentro de la unidad (por ejemplo, llamada `Prueba RAID 1 con fallo`).
>
> <img width="871" height="661" alt="image" src="https://github.com/user-attachments/assets/705b9683-55bc-41ce-8699-775676c6844f" />

### Reparación y sincronización del volumen RAID 1 con un nuevo disco

Para recuperar la tolerancia a fallos, romperemos primero la estructura degradada del espejo y posteriormente volveremos a asociar un nuevo disco de 6 GB (Disco 6).

1. **Eliminar el espejo averiado (Remove Mirror):**
   * En el **Administrador de discos**, hacemos clic derecho en la partición funcional del **RAID 1 (G:)** (Disco 4) y seleccionamos **Remove Mirror...** (Quitar espejo).
   * Aparecerá una ventana para seleccionar de qué disco deseamos retirar el espejo. Seleccionamos el volumen averiado o ausente (**Disco 5**) y pulsamos en **Remove Mirror**.
   * Confirmamos la acción haciendo clic en **Yes** en la ventana emergente de advertencia. El volumen quedará en el Administrador de discos como un volumen simple sin espejo.
>
> <img width="870" height="664" alt="image" src="https://github.com/user-attachments/assets/05b19a78-ce79-441e-93ee-5e10c867e702" />
> <img width="870" height="662" alt="image" src="https://github.com/user-attachments/assets/e19d5633-7c5f-4bf6-9d73-db8c04133cb7" />
> <img width="870" height="662" alt="image" src="https://github.com/user-attachments/assets/dc089a28-06ed-4f13-af7f-a397e36f19ab" />

2. **Añadir nuevo disco espejo (Add Mirror):**
   * Hacemos clic derecho sobre el volumen **RAID 1 (G:)** restante (Disco 4) y seleccionamos la opción **Add Mirror...** (Agregar espejo).
   * En la ventana emergente, elegimos el nuevo disco de sustitución de 6 GB disponible (seleccionamos el **Disco 6**, manteniendo el Disco 5 desconectado) y pulsamos en **Add Mirror**.
   * Confirmamos la conversión del nuevo disco a dinámico pulsando en **Yes**.
>
> <img width="868" height="662" alt="image" src="https://github.com/user-attachments/assets/0be8f0cc-a9e6-47df-9d25-85a46ec028c5" />

3. **Verificación del restablecimiento:**
   * El Administrador de discos iniciará el proceso de resincronización (**Resynching**) entre el Disco 4 y el nuevo Disco 6.
   * Esperamos a que la resincronización alcance el 100%. Una vez completada, el volumen **RAID 1 (G:)** volverá a mostrar el estado **Healthy** (Correcto), habiendo restaurado la redundancia con éxito.
>
> <img width="870" height="662" alt="image" src="https://github.com/user-attachments/assets/0db02d41-69c4-4490-a2ca-54618827acf5" />

---

## 7. Simulación y Recuperación ante Fallos en RAID 5

En este último apartado de la guía de laboratorio, evaluaremos el comportamiento del volumen RAID 5 ante la pérdida de uno de sus miembros físicos. Simularás la desconexión de una unidad, comprobarás la persistencia de la información gracias a los bloques de paridad distribuidos y repararás el conjunto reconstruyendo los datos en un nuevo disco de sustitución.

### Simulación de la avería en el RAID 5

1. Nos dirigimos nuevamente al **Administrador de discos** y localizamos los tres discos que componen el arreglo **RAID 5 (H:)** (Disco 7, Disco 8 y Disco 9).
2. Hacemos clic derecho sobre la etiqueta lateral de uno de los discos del RAID 5 (por ejemplo, el **Disco 9**) y seleccionamos la opción **Offline** (Desactivar) para simular su avería o desconexión física.
3. **Comprobación de la avería:** El Administrador de discos actualizará el estado del volumen **RAID 5 (H:)** mostrando el aviso **Failed Redundancy** (Redundancia con errores) en cada una de sus particiones, indicando que la matriz se encuentra en estado degradado.
>
> <img width="869" height="662" alt="image" src="https://github.com/user-attachments/assets/176d1504-0e8e-4a7d-906c-5137123f5c98" />

### Verificación de acceso a los datos en estado degradado

1. Abrimos el **Explorador de archivos** y accedemos al volumen **RAID 5 (H:)**.
2. Comprobamos que, pese a la pérdida del Disco 9, el volumen sigue plenamente operativo y la carpeta de prueba creada con anterioridad (`Prueba RAID 5`) se mantiene intacta y accesible.
3. Para verificar que el sistema permite operaciones de escritura en caliente sin redundancia, creamos una nueva carpeta de prueba dentro de la unidad (por ejemplo, llamada `Prueba RAID 5 fallando`).
>
> <img width="869" height="661" alt="image" src="https://github.com/user-attachments/assets/13e11431-40ee-4dec-818a-b331144ea592" />

### Reparación y resincronización del RAID 5 con un disco de sustitución

A diferencia del RAID 1, para recuperar un volumen RAID 5 no es necesario romper o eliminar el conjunto, sino utilizar la función nativa de reparación seleccionando un disco limpio de capacidad equivalente o superior (en nuestro laboratorio, el **Disco 10** de 8 GB).

1. **Verificación del disco de repuesto:**
   * Comprobamos en el **Administrador de discos** que disponemos del **Disco 10** de 8 GB en estado sin asignar (*Unallocated*).

2. **Ejecución de la reparación (Repair Volume):**
   * Hacemos clic derecho en cualquiera de los miembros no averiados que siguen activos en el **RAID 5 (H:)** (por ejemplo, el Disco 7 o el Disco 8).
   * Seleccionamos la opción **Repair Volume...** (Reparar volumen) del menú contextual.
   * En la ventana emergente **Repair RAID-5 Volume**, seleccionamos el disco que utilizaremos como reemplazo de la unidad caída (en este caso, el **Disco 10**) y pulsamos en **OK**.
>
> <img width="870" height="664" alt="image" src="https://github.com/user-attachments/assets/03d28919-97fa-460b-9766-dee95b8a40b3" />
> <img width="869" height="662" alt="image" src="https://github.com/user-attachments/assets/739ba1c6-677c-4022-abb3-f98357ee736a" />

3. **Confirmación y reconstrucción:**
   * Aparecerá una ventana de aviso indicando que el Disco 10 se convertirá en disco dinámico; confirmamos la operación haciendo clic en **Yes**.
   * El Administrador de discos iniciará automáticamente el proceso de resincronización y recálculo de paridad (**Resynching**) hacia el nuevo disco.
   * Esperamos a que la barra de progreso complete el 100%. Una vez finalizada la sincronización, el volumen **RAID 5 (H:)** volverá a mostrar el estado **Healthy** (Correcto), habiendo reparado la matriz con éxito y restaurado la redundancia frente a futuros fallos.
>
> <img width="869" height="664" alt="image" src="https://github.com/user-attachments/assets/de32d62c-48c2-439b-8430-35257b4e923e" />
