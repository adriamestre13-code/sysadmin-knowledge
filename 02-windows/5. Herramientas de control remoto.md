# Implementación de Herramientas de Control Remoto en Windows 11

En entornos de administración de sistemas y soporte técnico, la gestión a distancia de los equipos es un pilar fundamental para garantizar la continuidad del servicio y resolver incidencias de forma ágil. Este documento está concebido como una guía práctica y de laboratorio para desplegar, configurar y evaluar herramientas de control remoto sobre un entorno cliente con Windows 11 Pro.

Una herramienta de control remoto es un software que permite a un usuario o administrador acceder, gestionar y manipular la interfaz de un equipo informático a distancia a través de una red local (LAN) o de Internet. Mediante esta tecnología, el dispositivo local transmite las entradas del teclado y del ratón hacia el equipo remoto, recibiendo en tiempo real la señal de vídeo de su pantalla.

Estas herramientas sirven principalmente para:
* **Sistencia técnica y soporte:** Resolver problemas de software y configuraciones sin necesidad de desplazamiento físico.
* **Administración centralizada:** Gestionar servidores y puestos de trabajo dentro de la infraestructura corporativa.
* **Trabajo remoto:** Permitir a los usuarios acceder a sus puestos de trabajo habituales desde ubicaciones externas.
* **Transferencia y gestión de archivos:** Mover datos de forma segura entre distintas máquinas de la red.

---

## Objetivos del Laboratorio
* **Establecer conectividad de red:** Configurar dos máquinas virtuales con Windows 11 Pro dentro de un segmento de red local simulado y verificar la correcta comunicación entre ellas.
* **Implementar Escritorio Remoto de Windows:** Habilitar, configurar y documentar el uso de la herramienta nativa de control remoto de Microsoft para establecer una sesión remota.
* **Desplegar un software de terceros:** Instalar y configurar AnyDesk en los entornos cliente, ajustando parámetros esenciales de seguridad y uso.
* **Validar accesos remotos:** Comprobar y documentar paso a paso la conectividad activa y el control de la interfaz entre los sistemas.
* **Evaluar y justificar la elección de software:** Analizar los beneficios de despliegue y soporte entre soluciones integradas y de terceros para un entorno empresarial.

---

## Paso 1: Establecer conectividad de red entre equipos Windows 11

En este primer bloque del laboratorio se prepara el entorno de red entre dos máquinas virtuales con Windows 11 Enterprise (`Equipo 1` y `Equipo 2`), garantizando la comunicación bidireccional necesaria para el control remoto.

### 1.1. Configuración de la red virtual y comprobación de IPs iniciales

Para aislar el tráfico de prueba, ambas máquinas virtuales se configuran en la plataforma de virtualización dentro de la misma red interna (por ejemplo, en modo `Red interna` con el nombre `intnet` o en modo `Host-only`).

A continuación, se verifica el direccionamiento IP inicial asignado por el sistema en cada equipo mediante el Símbolo del sistema (`cmd`):

    ipconfig

* Equipo 1: Muestra una dirección de configuración automática APIPA (ejemplo: `169.254.113.87` con máscara `255.255.0.0`).
* Equipo 2: Muestra una dirección de configuración automática APIPA (ejemplo: `169.254.180.60` con máscara `255.255.0.0`).

---

### 1.2. Asignación de IP estática en cada equipo

Para asegurar una conectividad estable y predecible, se fuerzan direcciones IP estáticas en la configuración del adaptador de red Ethernet de cada máquina:

* **Configuración en Equipo 1:**
    * Asignación de IP: Manual (IPv4)
    * Dirección IPv4: `192.168.56.150`
    * Máscara de subred: `255.255.255.0`

* **Configuración en Equipo 2:**
    * Asignación de IP: Manual (IPv4)
    * Dirección IPv4: `192.168.56.200`
    * Máscara de subred: `255.255.255.0`

---

### 1.3. Configuración del Firewall de Windows

El Cortafuegos de Windows Defender bloquea por defecto las peticiones ICMP (ping) e incoming connections. Para permitir la comprobación de red, se desactiva el firewall en los perfiles de red o se crea una regla específica para habilitar las respuestas a paquetes ICMPv4-In.

1. Acceder a `Panel de control > Sistema y seguridad > Firewall de Windows Defender > Configuración avanzada` (o `Personalizar la configuración`).
2. Desactivar el Firewall de Windows Defender en las redes privadas y públicas en ambos equipos (o habilitar la regla de entrada de tráﬁco ICMP).

---

### 1.4. Comprobación de conectividad mediante Ping

Una vez configuradas las IPs fijas y ajustado el firewall, se valida la comunicación entre ambos clientes ejecutando el comando `ping` desde las consolas del Símbolo del sistema:

* **Prueba desde Equipo 1 hacia Equipo 2:**

    ping 192.168.56.200

  Se verifica la recepción correcta de los 4 paquetes enviados con 0% de pérdidas.

* **Prueba desde Equipo 2 hacia Equipo 1:**

    ping 192.168.56.150

  Se confirma la respuesta del Equipo 1 de forma transparente, garantizando la conectividad total a nivel de red para los siguientes pasos del laboratorio.
